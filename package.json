{
  "name": "vite-project",
  "version": "0.0.0",
  "scripts": {
    "build": "vite build",
    "test": "jest --config jest.config.js",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "jest": "^27.0.0",
    "jest-junit": "^12.0.0"
  }
}
